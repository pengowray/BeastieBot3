{
  "name": "beastiebot3-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Read-only Playwright smoke tests for the BeastieBot3 local web UI.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0"
  }
}
